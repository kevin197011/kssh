# frozen_string_literal: true

# config host list
@kssh_hosts = [
<% @data.each do |d| -%>
  { 'name': '<%= d[0] %>', 'ip': '<%= d[1] %>', 'username': 'root', 'method': 'key', 'passwdOrKey': 'id_rsa' }<%= ',' unless d == @data.last %>
<% end -%>
].freeze
